// Local-only mode: no external environment variables required.
// validateEnv is kept for compatibility but does nothing in local-only mode.
export function validateEnv() {
  // No validation required for local-only usage.
  return {};
}
